[project]
name = "watch-tool-watch-mark"
version = "0.1.0"
description = "Package a Python local lab for watch analysis with fixture event logs, golden state snapshots, and documented operating limits."
requires-python = ">=3.11"
